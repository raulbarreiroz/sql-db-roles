# Setup

This branch only has the overview README.

To run and test the projects, switch branches:

```bash
git checkout junior        # then open SETUP.md
git checkout semi-senior
git checkout senior
```

Each level branch includes a `SETUP.md` with install, run, and verification steps.
